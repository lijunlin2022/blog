软件项目使用不同的存储库来存储项目和
进化信息，例如源代码、错误和补丁。一个
结合了这些多个存储库的集成系统和
可以回答有关项目发展历史的广泛问题，这对软件开发人员和
研究人员。例如，源代码更改列表或列表
与错误修复相关的开发人员经常查询这两个问题
开发人员和研究人员。集成和收集这些信息是一个乏味、繁琐且容易出错的过程
手动，尤其是对于大型项目。以前的方法
问题使用框架将用户限制在一组预定义的
查询模板，或使用功能有限的查询语言。在这个
论文中，我们认为需要一个使用递归构建的框架
可枚举的语言，可以回答时间查询，并支持否定和递归。作为迈向这样一个框架的第一步，我们展示了一个我们构建的基于 Prolog 的系统，以及
对来自 Firefox 项目的真实世界综合数据的评估。
我们的系统允许优雅简洁但功能强大的查询，
并且可供开发人员和研究人员用于频繁的开发和实证分析任务。

软件项目比以往任何时候都大，它们的历史可以延续
比以往任何时候都长，因此开发人员无论何时都不知所措
面临程序理解或搜索等任务
通过项目的演化数据。 此类频繁开发任务的示例包括了解控制流、

找到函数之间的依赖关系，找到将
更改模块等时会受到影响。类似地，在软件维护期间，频繁的任务包括跟踪
由于错误修复而被更改，寻找适合修复错误的开发人员（例如，假设她已经修复了类似的错误）
过去或她曾在发生错误的模块上工作
在）。此外，一个允许查询集成的框架
大型项目的演化数据将有利于研究
经验软件工程，来自这些存储库的数据
常用于假设检验。所有这些任务都需要对跨存储库的大型数据集进行快速高效的搜索；
因此，一个可以整合来自多个数据的框架
来源和回答广泛的查询将是有益的
用于软件开发和实证分析。
虽然先前的框架允许有效的搜索和分析
软件进化数据，它们有两个主要的不便之处：（1）
它们不够灵活，例如，它们允许有限范围的
查询，或有固定的搜索模板； （2）它们不够强大，例如，它们不允许递归查询，或者不允许
支持否定；然而，这些功能是必不可少的广泛
搜索和分析任务的范围。在本文中，我们展示了我们如何
可以通过使用基于 Prolog 的集成来解决这些缺点
和查询框架。我们选择 Prolog 是因为它是声明式的
但功能强大，允许优雅、简洁地表达查询
用于数据收集和假设检验。我们的框架捕获了大量的历史软件演化数据（关于
错误，开发人员，源代码），并允许简洁而广泛
查询此数据。我们框架的三个主要创新点
是：（1）它是有时间意识的；我们数据库中的所有元组都有
允许比较进化数据的时间信息（例如，
文件的圈复杂度如何随时间变化？）；
(2) 支持否定、递归、量化等强大的语言特性； (3) 支持数据的高效整合
在存在不完整或缺失的情况下来自多个存储库
使用几种启发式的数据

Herraiz 等人。 [7] 确定了对有组织的软件存储库的需求，这些存储库可以改进软件工程中的数据检索技术，并确保可重复性、可追溯性和第三方独立验证和确认。 他们提出了一项研究
通过确定该领域的研究挑战来制定议程。
Hindle 和 German [8] 提出了 SCQL，这是一种用于源代码存储库的基于一阶和时间逻辑的查询语言。 他们的
数据模型是一个有向图，它捕获之间的关系
源代码修订、文件和修改请求。 SCQL 像我们一样支持通用和存在查询，但不支持我们所做的否定和递归。 虽然我们不亲

提出一种新的语言，显着的区别是我们认为
多个软件存储库以集成数据和回答查询。
我们的框架不仅捕获了源代码更改，还捕获了三个工件之间的关系：开发人员、错误和源代码。
菲舍尔等人。 [2] 提出了一种填充发布的方法
将源代码信息与 bug 结合起来的历史数据库
跟踪数据，因此能够查明丢失的数据
版本控制系统（例如合并点）未涵盖。与 Fischer 等人类似，我们最初通过提取
来自源代码和错误存储库的信息。
German [3] 提出使用软件轨迹来恢复软件演化历史——贡献者留下的信息，例如
如邮件列表、版本控制日志、软件版本、文档和源代码。该方法用于恢复习棉项目的软件进化特征。我们的数据收集
和数据库人口是相似的，尽管我们的框架意味着
回答查询聚合来自多个存储库的数据。
贝格尔等人。 [1] 开发了 Codebook，一个能够
在一个平台中组合多个软件存储库。我们的
工作类似，但主要挑战是建立一个框架
开源项目在于在没有组织的存储库和缺失数据的情况下收集和准确整合相关数据[2]。
他们的查询语言仅限于正则表达式，但
支持一组固定的预先计算的传递闭包结果；
我们使用 Prolog，一种图灵完备的语言，因此我们的框架
可以表达不受限制的查询（包括时间查询）。
努斯鲍姆等人。 [9] 介绍了终极 Debian 数据库
它集成了来自各种不同的 Debian 项目的信息
使用基于 SQL 的框架来回答与错误和源代码相关的用户查询。然而，他们的框架并没有
支持需要否定或传递闭包的查询。
斯塔克等人。 [11]对程序员的行为进行了实证研究
搜索活动以识别现有搜索的缺点
工具。他们发现基于 SQL 的最先进的源代码搜索
工具不足以有效地表达开发人员正在寻求的信息。我们相信声明式查询支持将
改善开发人员的代码搜索体验。
哈吉耶夫等人。 [6] 提出 CodeQuest，一种基于 Datalog 的代码
Java程序的搜索工具。他们使用了四个开源 Java
应用程序：Jakarta Regexp、JFreeChart、Polyglot 和 Eclipse
展示他们的工具。我们的工作与此有很大不同
以两种方式工作：（1）我们不构建任何特定于语言的工具，
从而形成了一个更广泛的框架，并且（2）我们整合了多个
存储库，它允许用户搜索有关
除了源代码之外的错误和开发人员。

现在转而介绍我们的框架。 我们首先激励
我们决定选择 Prolog 作为我们框架的存储和查询引擎，然后描述我们的关键新特性
方法，其次是数据模型。 我们在 DES 中实现了我们的框架，DES 是一个免费的、开源的基于 Prolog 的实现
一个基本的演绎数据库系统[10]。

Prolog 是声明性的。 在声明性语言中，查询简洁而优雅，因为不需要指定控制流或
预定义查询模板。
Prolog 支持否定。 否定扩展了可表达查询的范围，但可能很昂贵。 例如，以前的框架无法回答“返回未修复模块 A 中的错误的开发人员列表”或“返回列表”之类的查询
更改模块 A 时不受影响的模块数量”； 这样的
查询很有用，但是，例如，第二个查询可用于
减少回归测试。 表 2 中的查询 Q1 是
在我们的框架中使用否定。
Prolog 支持递归。 递归查询很重要，例如，
用于计算影响分析中所需的传递闭包。
尽管某些版本的 SQL 支持递归，但它通常是
有限形式的递归，并通过专有扩展实现。 表 2 中的 Q2 是一个需要递归的示例查询。

我们现在展示我们框架的一些关键特性；现存的
方法不支持这些特征中的一个或多个。
3.2.1 时间查询
以前从集成多个软件存储库构建数据库的方法无法解决时间问题
查询。例如，以下查询无法回答
通过现有系统：（1）谁在给定日期修改了文件 A？，（2）
在某个时期内，Bug B 被分配给了谁？，（3）什么
在特定时间段内对文件 F 进行了更改？，（4）
源代码指标（例如复杂性、缺陷密度）如何
文件随时间改变？
3.2.2 递归
传递闭包有助于影响分析，例如，“return the
将受文件 F 修改影响的文件集。”这
先前方法的问题是它们要么无法计算
传递闭包，或者只能在图（其中
边缘表示“依赖”关系）是静态已知的。为了
例如，我们可能想在之后找到文件 F 的所有后代
它已被重构。如果我们不知道“依赖”的定义，即在这种情况下，is-descendant-of，当时我们
构建数据库时，我们首先需要编写一个生成图的查询，然后使用一种足以表达传递闭包的强大语言来传递关闭它。同样，假设我们
文件 F 有 bug B1，我们要查找后续的列表
F 中可能在修复 B1 的过程中引入的错误。问题是，构建了后续的bug列表
动态的，例如，F 中的所有错误减去 F 中的错误列表
这取决于其他文件中的其他错误。以前的方法如
作为 Codebook [1] 使用预先计算的传递闭包来有效地
回答一组预定义的查询，例如，“所有函数的集合
F 取决于”；但是，诸如“列出所有功能
F1 和 F2 取决于”无法回答，因为它们需要

递归/传递闭包的语言支持。此外，当
来自新版本的数据被添加到数据库中，预先计算的传递闭包不起作用，因为“依赖”关系
可能由于新数据而改变，因此是动态传递的
需要闭包算法。
3.2.3 集成
在开源项目中，往往很难集成相关的
信息，因为它在空间上是分散的和不完整的。为了
例如，通常错误报告没有完整的信息
在错误修复期间更改的文件。考虑 Mozilla 错误
334314;根据 Bugzilla 错误报告，三个变化是
制作到文件 ssltap.c 以修复此错误 - 一次由开发人员 ID
alexei.volkov.bugs 和两次由开发人员 ID nelson 提供。补丁参考中有关此更改的信息不完整； 1
它是
不清楚谁做了哪些改变。然而，从变化
文件 ssltap.c 的日志，我们可以检索开发人员、更改和
更改时间戳，这有助于我们完成错误数据库。
3.3 存储
我们的框架旨在整合来自三个方面的信息
来源：(1) 源代码存储库——大小、位置、源代码
来自静态函数调用图等的依赖关系，（2）bug
存储库——谁报告了这个错误，目前的状态是什么
错误，错误依赖数据等，以及（3）之间的交互
开发人员——谁把 bug 扔给谁，两个开发人员工作
在相同的文件等上。注意函数调用、错误和开发人员的方式
交互导致依赖图。我们整合信息
从这三个来源中，并将其存储到数据库中，以便我们的
框架可以回答跨源查询，如
第 4 节。我们数据库的模式如表 1 所示。
我们现在继续描述数据库模式、内容和
更新。

源代码。源代码数据存储在三个表中：基本源代码信息、源代码更改和源代码
依赖关系。基础源码信息表（sourcebasic）
存储，对于每个模块（文件）：它的位置，它的功能列表
定义、复杂性度量、缺陷密度信息和相应的日期。请注意，一个文件可以有多个条目
由于多个版本的数据库，因此当文件未更改时
在发布中，除发布时间戳外的所有值都保持不变。
这些条目对于跟踪版本之间的更改很重要。
在源更改表（sourcechange）中，我们存储所有的详细信息
对文件进行的修订，无论是作为功能增强还是错误修复：更改日期、修订 ID、
错误 ID（如果更改是由于错误修复）和开发人员
谁提交了它，并添加了行数。对于源更改
在数据库中输入，我们还存储自
第一次提交2
当前的活动发生了。3
在源依赖表 (sourcedepend) 中，我们存储有关哪些其他
给定模块或函数直接依赖的实体，即文件，
调用图引起的模块或函数依赖关系。
错误。错误表（表 1 中的错误）存储相关信息
到错误：报告错误的日期，开发人员列表
与错误及其角色相关联（即，谁报告了它，谁
该错误在某个时候被分配给谁修复了它），严重性
错误，错误的现状，错误的最终解决方案和列表
此错误所依赖的错误数。回答有关时间间隔的查询（例如，在 2008 年 7 月至 5 月之间修复了多少错误
2010），我们添加了两个属性——DaysReported 和 DaysFixed——
表示自项目第一次发布以来的天数
该错误已分别报告和修复。如果一个错误没有
在创建数据库时已解决，DaysFixed 设置为
-1



开发者信息。感谢我们的源代码和错误表
架构设计选择，拥有开发人员数据库是多余的。
开发人员的所有信息（例如，折腾信息，错误
修复信息，代码作者信息）可以从
源代码和错误表。
更新数据库。随着软件的发展，我们的数据库
需要成长；注意数据库是单调递增的
（我们从不撤回事实）。
4. 例子
我们现在继续展示我们系统的用例——在软件开发和
实证研究。在表 2 中，我们演示了如何使用 Prolog
提高表现力并允许任意信息检索，
无需预先计算或模板。我们设想
这些查询构成了可以使用的查询库的内核
由开发人员在日常开发和维护活动中使用；同样，该库对研究人员进行实证研究也很有用
分析和假设检验。请注意，由于我们的查询语言基于 Prolog，因此我们直接支持存在查询
（Prolog 子句标题中的变量是存在量化的），和
通过重写的通用查询，即∀x Q(x) ⇔ ¬∃x ¬Q(x)。
5. 结果
我们随机选择了 2128 个 C 文件和 58 个 C++ 文件
Firefox 源代码存储库并提取其完整更改
记录历史以填充我们的源更改数据库。我们提取了
与这些源文件相关的 932 个错误。我们还添加到
我们的源依赖表 50 个函数调用边缘由
这些文件中函数之间的静态调用图。总的来说，我们的
数据库包含 63,142 个元组。在表 3 中，我们展示了查询
我们用来测试表 2 中显示的查询定义。第一个
列显示查询调用，第二列显示
结果元组的数量，4
第三列显示查询
执行时间，以毫秒为单位。我们发现花费的时间
使用DES回答一个查询随着数量的增加而增加
结果元组的数量，因此对于大查询的查询可能相当高
结果，例如 Q10；我们计划在未来的工作中解决可扩展性问题。
6. 未来的工作
我们目前正在使用 DES，一种基于开源 Prolog 的演绎数据库实现 [10] 作为我们框架的引擎。未来，我们计划使用 bdddbddb 框架
加快查询 [12]，因为 bdddbddb 已被证明能够
为大型真实世界程序处理基于 Datalog 的静态分析。我们计划使用其他软件特征/轨迹，例如邮件列表
信息，以改进我们的数据集以获得更准确的信息
建模和检索。在我们的初步实验中，如图所示
第 5 节，我们没有使用 sourcebasic 数据库或任何与之相关的查询。将来，我们想扩展我们的库来回答
与 sourcebasic 相关的查询，例如：“哪个文件在给定时间内表现出最大的复杂性或缺陷密度增加
间隔。”此外，我们计划跟踪引入错误的更改
使用我们的框架——导致错误的源代码更改。
最后，我们计划包括代码所有权信息以表明
哪个开发人员拥有我们的软件系统的哪个工件
数据库使用类似于 Girba 等人的启发式算法。 [4]。最后，我们
计划在我们当前帧的顶部添加一个可视化层 [5]